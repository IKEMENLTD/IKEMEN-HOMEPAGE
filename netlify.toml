[build]
  command = "npm run build"
  publish = "dist"

# Static legal pages must take precedence over SPA fallback.
[[redirects]]
  from = "/privacy-en"
  to = "/privacy-en.html"
  status = 200
  force = false

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
